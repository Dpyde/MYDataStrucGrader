#include <iostream>
#include <list>

struct Node
{

    // To store the Value or data.
    int data;

    // Pointer to point the Previous Element
    Node *prev;

    // Pointer to point the Next Element
    Node *next;

    // Constructor
    Node(int d)
    {
        data = d;
        prev = next = nullptr;
    }
};


int main(int argc, char const *argv[])
{   int num,index,type,temp;
    std::cin>>num>>index>>type;
    for (size_t i = 0; i < num; i++)
    {
        
    }
    
    
    
    



    }

