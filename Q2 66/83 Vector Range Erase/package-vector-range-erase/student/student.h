#ifndef __STUDENT_H_
#define __STUDENT_H_

// You can include library here
#include "vector.h"
#include <algorithm>
template <typename T>
void CP::vector<T>::range_erase(std::vector<std::pair<iterator, iterator>> &ranges) {

sort(ranges.end(), ranges.begin());
for (size_t i = 0; i < ranges.size(); i++)
{
    
}


}   

#endif
